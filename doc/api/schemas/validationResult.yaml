title: ValidationResult
type: object
required: ["ids"]
properties:
  ids:
    description: |
      IDs of the reaction SMARTS and products tested
    example: [ 27151406 ]
    items:
      type: integer
    type: array
  objects:
    $ref: "./validationObject.yaml"
